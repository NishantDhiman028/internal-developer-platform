apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodejs-service.fullname" . }}
  labels:
    {{- include "nodejs-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  - port: {{ .Values.observability.metricsPort }}
    targetPort: metrics
    protocol: TCP
    name: metrics
  selector:
    {{- include "nodejs-service.selectorLabels" . | nindent 4 }}
